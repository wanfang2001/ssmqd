export default {
      resturantName:(state)=>{
        return state.resturantName;
      },
      collapsed:(state)=>{
          return state.collapsed;
      },
      getJwt:(state)=>{
        return state.jwt;
      }
}
